import styled from '@emotion/styled';

const Wrapper = styled.div({
  position: 'relative',
  display: 'flex',
  flexDirection: 'column',
  minHeight: '100vh',
});

const Main = styled.main({
  display: 'flex',
  flexDirection: 'column',
});

export {
  Wrapper,
  Main,
};
